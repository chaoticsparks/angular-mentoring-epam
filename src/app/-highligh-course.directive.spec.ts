import { HighlighCourseDirective } from './-highligh-course.directive';

describe('HighlighCourseDirective', () => {
  it('should create an instance', () => {
    /*const directive = new HighlighCourseDirective();
    expect(directive).toBeTruthy();*/
  });
});
